// Hex tokens mirrored from tailwind.config so Recharts (which needs concrete
// colors) stays in sync with the design system.
export const CHART = {
  primary: '#4f46e5',
  primarySoft: '#a5b4fc',
  success: '#10b981',
  warning: '#f59e0b',
  danger: '#ef4444',
  info: '#0ea5e9',
  grid: '#e2e8f0',
  axis: '#94a3b8',
};

// Category donut palette — distinct but harmonious (not random pastels).
export const CATEGORY_PALETTE = ['#4f46e5', '#0ea5e9', '#10b981', '#f59e0b', '#ec4899', '#8b5cf6', '#64748b'];
