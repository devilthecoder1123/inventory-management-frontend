// Hex tokens mirrored from tailwind.config so Recharts (which needs concrete
// colors) stays in sync with the design system.
export const CHART = {
  primary: '#5d67c4', // muted indigo accent
  primarySoft: '#abb2e6',
  success: '#16a34a',
  warning: '#d97706',
  danger: '#dc2626',
  info: '#0284c7',
  grid: '#e4e4e7', // neutral-200 (zinc)
  axis: '#a1a1aa', // neutral-400
};

// Curated categorical palette — distinguishable but harmonious and slightly
// desaturated (no neon, no random pastels).
export const CATEGORY_PALETTE = ['#4b53a8', '#3f7fa6', '#4f9d80', '#c08a3e', '#b06a8c', '#7d7fb5', '#a1a1aa'];
